[package]
name = "magic-structure"
version = "0.1.0"
edition = "2021"

[lib]
name = "magic_structure"
path = "src/lib.rs"

[dependencies]
crossbeam-queue = "0.3"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "mutation_benchmarks"
harness = false
